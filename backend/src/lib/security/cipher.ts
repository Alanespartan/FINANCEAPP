// import { cipherKey } from "@backend/app";
// import CryptoJs from "crypto-js";

// export function encrypt(plainText: string) {
//     return CryptoJs.AES.encrypt(plainText, cipherKey).toString();
// }

// export function decrypt(encryptedText: string) {
//     const bytes = CryptoJs.AES.decrypt(encryptedText, cipherKey);
//     return bytes.toString(CryptoJs.enc.Utf8);
// }

// use https://nodejs.org/api/crypto.html#cryptoscryptpassword-salt-keylen-options-callback
// don't use CrytoJs
// salt
